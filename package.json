{
  "private": true,
  "name": "cv",
  "type": "module",
  "scripts": {
    "init": "resumed init",
    "start": "npm start",
    "build": "mkdir -p dist && resumed --theme jsonresume-theme-even -o dist/index.html"
  },
  "dependencies": {
    "jsonresume-theme-even": "^0.23.0",
    "resumed": "^6.0.0"
  }
}
